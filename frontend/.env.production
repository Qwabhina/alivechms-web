# Production environment
# API lives at the domain root (www.onechurch.com/auth/login, /member/all, etc.)
# Using "/" as base means Axios sends root-relative requests regardless of
# which subdirectory the built SPA is served from (e.g. /public/).
VITE_API_BASE_URL=/
VITE_APP_TITLE=AliveChMS
