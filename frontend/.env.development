# Development environment
# Requests prefixed /api are caught by the Vite proxy and forwarded to the
# production backend at www.onechurch.com. The proxy strips the /api prefix
# before forwarding, so auth/login becomes www.onechurch.com/auth/login.
VITE_API_BASE_URL=/api
VITE_APP_TITLE=AliveChMS — Development
